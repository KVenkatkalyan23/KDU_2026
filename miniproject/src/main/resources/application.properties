spring.application.name=jwtTokenAuth


spring.datasource.url=jdbc:mysql://localhost:3306/smarthome
spring.datasource.username=root
spring.datasource.password=venkat@23
spring.datasource.initialization-mode=always

# Make Spring always run SQL scripts even on MySQL


# Make Hibernate create missing tables
spring.jpa.hibernate.ddl-auto=update
spring.sql.init.mode=always

# Show SQL logs (optional, helps debug)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

